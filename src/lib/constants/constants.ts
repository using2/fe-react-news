import { getHeadline } from '@/lib/utils/headline';

export const DIRECTION = {
  LEFT: 'left',
  RIGHT: 'right',
};

export const NEWS_HEADLINE_DATAS = getHeadline();

export const VIEW = {
  grid: 'grid',
  list: 'list',
};

export const GRID_VIEW_ITEM_COUNT = 24;
